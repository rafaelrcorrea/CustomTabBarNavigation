import React, {Component} from 'react';
import {Text, View} from 'react-native';
import styles from './styles';

const Left = () => (
  <View style={styles.container}>
    <Text style={styles.welcome}>TAB LEFT</Text>
  </View>
);

export default Left;