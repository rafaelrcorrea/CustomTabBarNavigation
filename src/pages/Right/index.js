import React, {Component} from 'react';
import {Text, View} from 'react-native';
import styles from './styles';

const Right = () => (
  <View style={styles.container}>
    <Text style={styles.welcome}>TAB RIGHT</Text>
  </View>
);

export default Right;